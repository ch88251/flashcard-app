# Admin credentials
ADMIN_PASSWORD=
ADMIN_SECRET=
ADMIN_USERNAME=

# PostgreSQL Configuration
# For local development with docker-compose, use these defaults:
PGHOST=localhost
PGPORT=5432
PGUSER=flashcards
PGPASSWORD=changeme
PGDATABASE=flashcards

# Alternatively, use a connection string (e.g., for hosted databases like Neon):
# DATABASE_URL=postgres://user:password@host:port/database
